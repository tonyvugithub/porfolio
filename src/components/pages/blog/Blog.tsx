import React from 'react';
import styled from 'styled-components';

const BlogWrapper = styled.div``;

const Blog = () => {
  return (
    <BlogWrapper>
      <h1>Blog</h1>
    </BlogWrapper>
  );
};

export default Blog;
